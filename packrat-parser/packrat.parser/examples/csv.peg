# PEG Example for CSV files
#

# This is an example that parses a CSV file
XYZ -> THIS
123 <- TEST

START <- FILE

FILE  <- LINE ( EOL LINE )*
LINE  <- ITEM ( COMMA ITEM )*
CELL  <- (!COMMA !EOL . )* 

EOL   <- SP* [\x13+\x10]
EOF   <- SP* !.
COMMA <- ","


